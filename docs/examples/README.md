# Example Super Search pages

These are real exported Super Search pages, generated by the builder from the templates
in `../templates/`. Open one and type a query into its search box: every engine button
updates to search for that query on its own site.

- `supersearch-research.html` — exported from `templates/research.json`.

To regenerate, load the matching template in the builder and use **Save Super Search page**.
